<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.igym">
    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"

        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.IGym"
        tools:targetApi="31">

        <activity
            android:name=".ui.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.InicioApp"></activity>
        <activity android:name=".ui.Registro"></activity>
        <activity android:name=".ui.Horarios"></activity>
        <activity android:name=".ui.Tarifas"></activity>
        <activity android:name=".ui.AddAct"></activity>
        <activity android:name=".ui.SelecAct"></activity>
        <activity android:name=".ui.webView"></activity>
        <activity android:name=".ui.misActividades"></activity>
        <activity android:name=".ui.EditAct"></activity>


    </application>

</manifest>